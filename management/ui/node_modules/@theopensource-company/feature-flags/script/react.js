"use strict";
"use client";
Object.defineProperty(exports, "__esModule", { value: true });
exports.featureFlagsHookFactory = exports.FeatureFlagProvider = exports.FeatureFlagContext = void 0;
const react_1 = require("react");
const mod_js_1 = require("./mod.js");
exports.FeatureFlagContext = (0, react_1.createContext)({
    featureFlags: new mod_js_1.FeatureFlags({ schema: {} }),
    state: [{}, () => { }],
});
function FeatureFlagProvider({ children, hydratedOverrides, ...options }) {
    const featureFlags = options.featureFlags ||
        new mod_js_1.FeatureFlags(options.options);
    const jsonState = (0, react_1.useSyncExternalStore)((listener) => {
        featureFlags.subscribe(listener);
        return () => featureFlags.unsubscribe(listener);
    }, () => JSON.stringify(featureFlags.store), () => JSON.stringify(featureFlags.initialStore));
    const state = JSON.parse(jsonState);
    function setState(updates) {
        const flags = Object.keys(updates);
        flags.forEach((flag) => {
            const v = updates[flag];
            if (mod_js_1.FeatureFlags.isValidValue(v)) {
                featureFlags.set(flag, v);
            }
        });
    }
    (0, react_1.useEffect)(() => {
        if (!hydratedOverrides)
            return;
        const flags = mod_js_1.FeatureFlags.listOptionsFromSchema(featureFlags.schema);
        flags.forEach((flag) => {
            const override = hydratedOverrides(flag);
            if (mod_js_1.FeatureFlags.isValidValue(override)) {
                try {
                    featureFlags.set(flag, override);
                }
                catch (_e) {
                    /* ignore readonly errors here */
                }
            }
        });
    }, []);
    return (0, react_1.createElement)(exports.FeatureFlagContext.Provider, {
        value: {
            featureFlags,
            state: [state, setState],
        },
    }, children);
}
exports.FeatureFlagProvider = FeatureFlagProvider;
function featureFlagsHookFactory(_) {
    return () => {
        const { state } = (0, react_1.useContext)(exports.FeatureFlagContext);
        return state;
    };
}
exports.featureFlagsHookFactory = featureFlagsHookFactory;
